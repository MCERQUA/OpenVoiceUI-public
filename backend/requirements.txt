flask>=3.0.0
flask-cors>=4.0.0
flask-sock>=0.7.0
websockets>=13.0
python-dotenv>=1.0.0
google-generativeai>=0.8.0
psutil>=5.9.0
requests>=2.31.0
faster-whisper>=1.0.0
# Groq TTS (Orpheus) — required if using Groq as TTS provider
groq>=0.8.0
# Clerk JWT authentication
PyJWT>=2.8.0
cryptography>=42.0.0
# Supertonic TTS dependencies (optional — install manually if using local TTS)
# onnxruntime>=1.23.1
# soundfile>=0.12.1
# librosa>=0.10.0
